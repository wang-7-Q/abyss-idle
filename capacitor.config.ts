import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.incrementaldungeon',
  appName: 'incremental-dungeon-game',
  webDir: 'dist'
};

export default config;
